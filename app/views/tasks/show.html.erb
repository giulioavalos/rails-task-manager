<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Document</title>
</head>
<body>

<div class="container">
  <h1 class="mt-5 mb-4">Task Details</h1>

  <div class="card">
    <div class="card-body">
      <h5 class="card-title"><%= @task.title %></h5>
      <p class="card-text"><%= @task.details %></p>
      <p class="card-text">Completed: <%= @task.completed ? 'Yes' : 'No' %></p>
    </div>
  </div>

  <%= link_to 'Edit', edit_task_path(@task), class: "btn btn-primary mt-3" %>
  <%= link_to 'Back to Tasks', tasks_path, class: "btn btn-secondary mt-3" %>
</div>

</body>
</html>
